<!DOCTYPE html>
<html>
  <head>
    <title>Sample6</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <nav class="navbar navbar-expand-lg navbar-dark  sticky-top" style="background-color: #563d7c;">
    <div class="container">
      <a class="navbar-brand" href="#">BSC-PTSP</a>
       <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <% if user_signed_in? %>  
        <ul class="navbar-nav">
          <li class="nav-item active">
            <%= link_to 'wall', home_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
          
            <% if !current_user.username.nil?  %>
              <%= link_to current_user.username, current_user, class: "nav-link" %>
            <% else %> 
              <%= link_to 'my account', current_user, class: "nav-link" %>
            <% end %>
          
          </li>

          <li class="nav-item">
            <a class="nav-link" href="<%= edit_user_path(current_user) %>">
              Edit Profile
            </a>
          </li>
        </ul>

        <button class="btn btn-outline-warning my-2 my-sm-0" >
          <% if @user.role == "manager" %>
            Tạo thông báo
          <% else %>
            Gửi yêu cầu
          <% end %>
        </button>
        <% end %> 
      </div>

    </div>
    </nav>
    <div class="container">


      <% if notice %>
        <aside id="notice"><%= notice %></aside>
      <% end %>

      <% if alert %>
        <aside id="alert"><%= alert %></aside>
      <% end %>
      
      <%= yield %>
    </div>
  </body>
</html>
